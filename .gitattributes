# Normalize line endings: store everything as LF in the repo; only Windows
# batch files use CRLF on checkout.  Keeps build.sh / bundle_dlls.sh valid on
# Linux regardless of the contributor's OS.
* text=auto eol=lf
*.bat text eol=crlf
